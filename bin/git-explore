#!/usr/bin/env ruby

require "bundler/setup"
require "git_explorer"

explorer = GitExplorer::Explorer.new
explorer.explore_light(ARGV[0])
# TODO find an way to pass --light as parameter
# explorer.explore(ARGV[0])

